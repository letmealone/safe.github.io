# 안전워치 UX 검토·시안 작업 진행기록 및 재개 안내

기록일: **2026-09-15, 한국시간**  
작업공간: `/Users/donghyukyi/Desktop/dowon`  
결과물 폴더: `/Users/donghyukyi/Desktop/dowon/safetywatch_ux_review`

## 1. 다음 세션에서 먼저 읽을 요약

**최신 요청은 점검 현황·점검 이력에서도 BIM에 자연스럽게 직접 접근하도록 보강하는 것이다.** 이 보강을 구현하고 검증했다. 재로그인용 기록 요청 이후 추가된 작업이며, 최신 변경은 아래 13절을 먼저 확인한다. 새 지시 없이 추가 개편을 시작할 필요는 없다.

핵심 방향은 **노후건축물 안전진단 담당자의 업무 우선**이다. 일반 지도 플랫폼처럼 모든 사용자를 지도에 먼저 보내지 않는다.

- 초기화면 왼쪽: 지자체 공무원·전문기관의 업무 로그인.
- 로그인 후: 개인 작업 대시보드. 직전 작업과 조회조건을 이어가면서 현재 요청·변화도 함께 확인.
- 업무 화면 상단: `내 업무 / 지도 분석`을 별도 탭으로 제공.
- 초기화면 오른쪽: 보조 경로인 비로그인 공개 정보조회. 공개 지도와 공개용 외형 모델 제공.
- 지도: 지역 클릭·주소 검색·휠 확대를 병행하고, 대상 선택 → 고정 요약 패널 → 넓은 sBIM 결함 검토로 연결.
- 돌아가기: 같은 건물·회차·선택 근거와 지도 범위·검색조건을 유지.

최신 시안은 [workflow.html](workflow.html), 전체 설명은 [UX_개편_제안서.md](UX_개편_제안서.md), 화면별 검토는 [screen_gallery.html](screen_gallery.html)이다.

## 2. 반드시 유지할 사용자 요구사항과 허용 범위

1. 주요 사용자는 **BLCM(건축물 생애이력 관리시스템)에 익숙한 지자체 공무원, 점검·진단 전문기관 담당자**다.
2. 목적은 단순한 외형 개선보다 **업무 순서, 대상 찾기, 근거 확인, 보고서·검토 연결의 UX 개선**이다.
3. 대상은 일반 건축물 전체가 아니라 **안전진단 관리가 필요하거나 그 필요성을 검토할 노후건축물**이다. 서비스 제목·안내도 이를 명확히 한다.
4. 사용자는 자신의 아이디어를 정답으로 전제하지 말고 객관적으로 검토하기를 요청했다. 채택·보완 이유를 쉽게 설명한다.
5. 시나리오는 공통 메인·로그인부터 시작하여 업무의 처음부터 끝까지 이어져야 한다.
6. 제안서에는 **기존 화면의 문제 → 사용자가 겪는 불편 → 대안과 이유**를 비전문가가 이해하기 쉽게 기록한다.
7. 실제 홈페이지 탐색은 Chrome에서 사용자가 볼 수 있게 한다. Chrome 확장프로그램 사용도 허용받았다.
8. **실제 사이트에서는 UI를 열어보는 정도만 허용된다. 실제 업무 데이터 입력·저장·업로드·수정·삭제는 금지된다.** 이전에 허용된 로그인은 이와 구별한다.
9. 로컬 HTML 시안의 가상 상태 변경·가상 입력은 실제 시스템과 분리되어 있다. 실제 서버로 전송하지 않는다.
10. 사용자 요청에 따라 문서 자문 에이전트가 참여했다. 기존 자문을 먼저 활용하고, 대리 작업이 다시 필요하면 범위가 명확한 자문으로 한정한다.

실제 로그인 URL:

```text
https://safetywatch.duckdns.org/safety/adminsys/loginPage?redirect=https%3A%2F%2Fsafetywatch.duckdns.org%2Fsafety2%2Fauth%3Ftarget%3D%252F
```

실제 사이트 로그인 자격증명·쿠키는 이 인계 파일이나 시안 상태 백업에 기록하지 않았다.

## 3. 지금까지 수행한 검토

### 실제 시스템과 BLCM 검토

- 사용자 요청의 30분 이상 탐색은 최초 13:26~13:57의 실제 화면 관찰로 수행했고, 이후 추가 화면도 확인했다. 코딩 시간을 실제 사이트 탐색 시간으로 계산하지 않았다.
- 안전워치 관리자 화면, 건축물·점검 목록, BIM·현장 결함·회차 비교, SSIB 보고서와 연결 확인 화면을 살펴봤다.
- BLCM의 공식 자료와 사용자 업무 흐름을 참고했다. 실제 법정 접수·승인 연계나 SSO가 구현되어 있다고 단정하지 않았다.
- 실제 관찰 화면은 `evidence/`, 관찰 근거 연결은 `evidence_index.html`과 제안서에 있다. 이 폴더에는 실제 개발 화면과 로컬 시안 캡처가 함께 있으므로 URL·설명을 보고 구별한다.

### 개발계획 문서 자문

사용자 제공 PDF는 28쪽의 `20260903 국토부 중간점검_안전AI_fin.pdf`다.

실제 원본 경로:

```text
/Users/donghyukyi/Desktop/노후건축물/0기획/발표자료/20260903 국토부 중간점검_안전AI_fin.pdf
```

문서 자문 에이전트: `/root/platform_document_advisor`. 재접속 후 이 에이전트의 세션이 유지된다고 가정하지 않는다. 자문은 [document_advice.md](document_advice.md), [full_scenario_advice.md](full_scenario_advice.md)에 남아 있다.

주요 해석:

- 19쪽: 행정업무·서비스·R&D 기술을 구분한다. 기술 메뉴보다 사용자 업무를 중심으로 구성할 근거.
- 13·21쪽: BIM의 부재정보·이력·개별 및 지역 대상 연결.
- 27쪽: 현재 개발의 현장점검·결함 매핑 등 활용할 기능.
- 선행 안전워치 개발과 후속 AI 사업 목표를 구분한다. 연구 목표 수치를 현재 검증 성능으로 인용하지 않는다.
- 상세 결함·사진을 포함한 원본 sBIM 전체의 비로그인 공개 정책은 확인되지 않았다.

초기 자문에는 “브라우저 메모리에서만 상태 변경”이라는 당시 전제가 있다. **최신 시안은 로컬 브라우저 저장소에도 가상 상태를 보관한다.** 현재 구현·범위 판단은 이 재개 문서와 최신 사용 안내를 우선한다.

## 4. 검토 결론과 채택한 설계

| 쟁점 | 반영 방향 | 이유 |
|---|---|---|
| 로그인 후 시작점 | 개인 대시보드, 최근 작업과 현재 요청 병행 | 매일 같은 대상을 다시 찾는 부담을 줄이고 새 보완 요청도 놓치지 않음 |
| 업무와 지도 관계 | `내 업무 / 지도 분석` 분리 | 처리 업무와 지역 탐색의 목적이 다름 |
| 초기화면 사용자 우선순위 | 업무 왼쪽·강조, 공개 조회 오른쪽·보조 | 주 사용자에게 익숙한 업무 입구를 먼저 제공 |
| 단계별 지도 탐색 | 전국 → 시·도 → 실제 하위 시·군·구 → 읍·면·동 | 지역별 행정구조 차이를 수용. 알고 있는 주소는 바로 검색 |
| 지도 기본 범위 | 업무용은 직전·관할·담당 범위, 공개용은 전국 | 업무 담당자에게 전국부터 재탐색을 강요하지 않음 |
| 확대와 조회 | 확대·이동은 시점 변경, 조회 범위 변경은 별도 동작 | 휠을 움직였다는 이유로 조회조건이 바뀌지 않음 |
| 건물 상세 | 지도 옆 고정 요약 패널, 필요 시 sBIM 확장 화면 | 작은 팝업 안에 모델·사진·긴 표가 겹치는 불편 감소 |
| 결함 검토 | 목록 ↔ 모델 표시 ↔ 층 ↔ 근거 동기화 | 어느 기록의 어떤 결함인지 다시 찾지 않음 |
| 익명 공개 | 공개 요약과 공개용 외형 모델 | 내부 결함·사진·검토 의견의 공개를 임의로 전제하지 않음 |
| 이전 화면 | 실제 탐색 이력과 맥락 복원 | 상위 지역 이동과 과거 화면 복귀를 구분 |

## 5. 현재 구현 완료 범위

### 처음부터 끝까지의 업무 흐름

- 공통 메인 → 역할별 가상 로그인 → 개인 대시보드.
- 전문기관의 대상 확인·현장 근거 확인·의견 정리·보고서 v1 공유.
- 지자체의 공유 결과 검토·보완 요청.
- 기관의 보완 설명 작성·v2 공유 → 지자체 재검토·이력 확인.
- 건물·건축물 동·업무유형·회차를 유지하며 목록·근거·보고서를 연결.
- 로그인 전 선택한 업무 목적지를 로그인 후 연결.
- 가상 권한 범위: 지자체 3개 건물, 기관 2개 건물. 범위 밖 업무 직접 진입도 차단.
- 직전 작업·조회조건·가상 초안 및 지도 탭의 환경을 재로그인 후 복원.

### 지도

- 남한 17개 시도 경계와 클릭 탐색.
- 전체 대표 경로: `전국 → 경기 → 고양시 → 일산동구 → 장항1동/장항2동/마두1동`.
- 다른 시도나 사례 없는 구역도 열 수 있고 자료 없음·예시 지역 복귀를 안내.
- 주소·건물명 검색, 실제 마우스 휠·드래그, 확대·이동 버튼·키보드 조작.
- 단일 검색 결과도 주변 지역이 보이는 기본 범위를 유지. 사용자가 직접 휠로 더 확대하는 것은 허용.
- 조회 전체 수와 현재 화면 안 대상 수를 구분하여 표시.
- `이 화면 범위에서 조회`는 클릭 당시 범위를 확정한다. 이후 이동만으로 결과가 달라지지 않음.
- 건물 선택 시 고정 요약 패널: 대상 식별, 행정동/건축물 동 구분, 회차·자료 상태·다음 확인·업무 및 sBIM 진입.

### sBIM

- 결함 세 항목 목록을 모델 위에 배치.
- 목록과 모델 표시를 양방향 연결. 층을 바꾸면 해당 층의 첫 결함·근거도 함께 선택.
- F-03 옥상 항목은 옥상 근거 도식으로 표시.
- 이전 회차 관찰값·이번 회차 비교, 보고서 현재본·공유 원문 버전 선택.
- 대략 위치와 연결 위치를 구분. 수치 변화만으로 안전이나 보수 완료를 판정하지 않음.
- 모델 미등록·로딩 실패 시에도 기존 점검자료 경로 제공.
- 다른 건물에 주민회관의 보고서 버전·층수가 섞이지 않도록 수정.
- 지도 복귀 시 원래 조회조건·시점·선택 건물 요약 복원.
- 공개 지도와 공개 상세는 공통 공개 기록 기준을 사용하도록 통일.

문서 자문 에이전트의 마지막 지적 세 가지(과도한 자동 확대, 층과 근거 불일치, 공개 기록 유무 불일치)는 모두 반영했다.

## 6. 가상 자료와 실제 자료의 경계

**완성된 전국 실데이터 GIS 또는 실제 IFC 뷰어가 아니다.** 현재 결과물은 UX를 검토하는 로컬 HTML/CSS/JavaScript/SVG 시안이다.

- 시·도 경계: 2021년 공개 경계, 고양시 경계: 2020년 공개 경계의 단순화.
- **구·동 도형은 실제 행정경계가 아닌 설명용 구획**이다.
- 전국 모든 시군구·읍면동을 구현한 것이 아니라 대표 경로를 구현했다.
- 세 건물의 주소·위치·점검자료·근거 그림·모델은 가상이다.
- 지도 색·숫자는 가상 사례 분포이며 위험도·안전등급이 아니다.
- 실행 중 외부 지도 타일·API·실제 BIM·운영 데이터 요청이 없다.
- 출처와 가공 범위: [reference/map/README.md](reference/map/README.md).

가상 사례:

| ID | 건물 | 위치 예시 | 자료 특징 |
|---|---|---|---|
| 1 | 예시 중앙상가 · 가동 | 고양시 일산동구 장항2동 · 예시 중앙로 10 | 지상 3층, 정기점검, 모델 있음·보고서 연계 미확인 |
| 2 | 예시 주민회관 · 본동 | 고양시 일산동구 장항1동 · 예시 안전로 20 | 지상 2층, 소규모 노후건축물 점검, 주 업무 시나리오·결함 3개 |
| 3 | 예시 문화시설 · 나동 | 고양시 일산동구 마두1동 · 예시 문화로 30 | 지상 1층, 연결 확인 후보, 모델 없음 |

## 7. 검증 완료 내용

| 결과 파일 | 확인 수 | 범위 |
|---|---:|---|
| [entry_validation.json](entry_validation.json) | 25 | 진입·업무·공개 조회·재접속 흐름 |
| [navigation_validation.json](navigation_validation.json) | 20 | 이전 화면·브라우저 이력·조건·초안·시점 복원 |
| [map_validation.json](map_validation.json) | 60 | 지도·sBIM 동작과 데스크톱·모바일 표시 |
| 합계 | **105** | 가상 시안 동작·표시 확인 모두 통과 |

추가로 전체 42개 갤러리 화면을 다시 캡처하고 1440px 문서 폭을 확인했다. 갤러리 검증 결과는 [gallery_validation_v2.json](gallery_validation_v2.json)에 있다. 갤러리의 중복 폭 확인을 위 105개에 다시 더하지 않았다.

- 지도 확인에는 실제 마우스 휠·마커 클릭·구역 위 드래그도 포함.
- 1366×768과 390×844에서 지도·모델 주요 화면 가로 넘침 확인.
- 1366×768 첫 화면에서 결함 세 항목 선택 가능 확인.
- 로컬 문서·시안 링크 누락 없음 확인.
- 갱신 PDF는 23쪽. 최신 `업데이트 후 확인`과 105개 확인 내용 포함 검증.

**이 결과는 실제 담당자 대상 사용성 평가 또는 운영 서버 인증·인가 검증 결과가 아니다.** 이미 통과한 확인을 재접속했다는 이유만으로 모두 다시 실행할 필요는 없다. 변경한 부분과 관련된 확인만 수행한다.

## 8. 재접속·재로그인 시 이어가는 방법

### Codex 작업 대화 재개

같은 `/Users/donghyukyi/Desktop/dowon` 작업공간을 연다. 작업공간의 `AGENTS.md`에 이 파일을 먼저 읽도록 안내를 남겼다.

필요하면 다음 문장을 보내면 된다.

> `/Users/donghyukyi/Desktop/dowon/safetywatch_ux_review/진행기록_및_재개안내.md`를 읽고 안전워치 UX 시안 작업을 이어서 진행해 주세요. 실제 시스템은 조회만 허용합니다.

새 대화가 과거 대화를 자동으로 기억한다고 가정하지 않는다. 이 파일과 연결된 로컬 결과물을 인계의 기준으로 사용한다.

### 시안의 가상 계정 재로그인

1. 같은 Chrome 프로필에서 [workflow.html](workflow.html)을 연다.
2. 초기화면 왼쪽 `업무시스템 로그인`으로 들어가 이전 역할을 선택한다.
3. `로그인 후 메인화면 보기` → 개인 대시보드 → 최근 작업 또는 `지도 분석`을 선택한다.
4. 가상 상태는 `localStorage`의 `safetywatch-ux-v2` 키에 저장되어 있다. 실제 사이트 로그인 세션과 무관하다.

`?tour=숫자`로 여는 갤러리 단계는 독립 미리보기이며 보관 기록을 갱신하지 않는다. 일반 체험은 매개변수 없는 `workflow.html`에서 시작한다.

### 이번 기록 시점의 백업

[재개_시점_시안상태.json](재개_시점_시안상태.json)에 **2026-09-15 17:23:38 KST**의 가상 저장소와 조회 맥락을 백업했다. 실제 사이트 쿠키·비밀번호·운영 데이터는 포함하지 않았다. Chrome 화면이나 저장소를 변경하지 않고 읽어서 백업했다.

- 읽은 화면: 공무원 역할의 `내 업무 대시보드(home)`.
- 선택 업무: 주민회관 ID 2, 보고서 v1 공유 상태.
- 지도: 장항1동·주민회관 선택. 사용자가 조작한 확대·이동 값 그대로 백업.
- 모델 맥락: 2026년 회차, F-03, 전체 층.

이 백업은 브라우저 저장소가 사라졌을 때 복구에 참고하는 파일이다. **최신 브라우저 상태를 자동으로 덮어쓰지 않는다.** Chrome 프로필 또는 파일 경로가 달라지면 저장소가 그대로 보이지 않을 수 있으므로 필요할 때만 백업을 로컬 시안에 복원한다. 실제 사이트에는 복원하지 않는다.

## 9. 파일 구조와 수정 시 주의할 지점

| 파일 | 역할 |
|---|---|
| `workflow.html` | 최신 클릭 가능한 시안 시작점 |
| `prototype.js`, `prototype.css` | 기존 업무 흐름·공통 화면·기본 스타일 |
| `entry_extension.js` | 공통 메인·로그인·대시보드·공개 조회·역할과 가상 저장소 |
| `map_geometry.js` | 로컬 지역 경계·예시 구획 |
| `map_extension.js`, `map.css` | 새 지도·조회·선택 요약·지도 상태 보관 |
| `work_model.js` | 결함 위치 모델·층·근거·회차 비교·지도 연결·새 갤러리 단계 |
| `navigation.js` | 이전 화면·브라우저 이력·맥락 및 작성 초안 복원 |
| `bootstrap.js` | 확장 스크립트 로드 이후 초기화와 미리보기 진입 |
| `screen_gallery.html`, `mockups_v2/` | 최신 47개 화면과 개편 이유 |
| `UX_개편_제안서.md`, `report.html`, PDF | 전체 UX 분석과 제안 |
| `사용자유형별_진입_보강안.md`, `entry_review.html` | 사용자별 진입·직전 환경 복원 논리 |
| `지도_sBIM_UX_검토.md`, `map_review.html` | 지도·sBIM 검토·실제 반영 범위 |
| `시안_사용_안내.md` | 사용자 체험 동선·지도 및 모델 조작 안내 |
| `reference/`, `evidence/` | 참고자료·관찰 기록 |
| `versions/v1/` | 이전 시안 보관. 최신 버전 아님 |

스크립트 로드 순서:

```text
prototype.js → entry_extension.js → map_geometry.js → map_extension.js
→ work_model.js → business_bim.js → navigation.js → bootstrap.js
```

여러 확장이 전역 `go`, `render`, `navContext`, `showTourStep` 등을 감싸는 구조다. 순서를 바꾸거나 초기화를 앞당기면 지도·권한·뒤로가기·미리보기가 어긋날 수 있다.

**과거 일회성 스크립트** `refine_outputs.py`, `add_login_example.py`를 무심코 재실행하지 않는다. `verify_prototype.py`와 `prototype_validation.json`은 이전 범위의 자료이며 최신 검증은 위 3개 스크립트·결과 파일을 기준으로 한다.

## 10. 브라우저·검증 도구 재개 참고

검토 당시 환경:

- Python: `/opt/miniconda3/bin/python3` (`requests`, `websocket`, `pypdf`, `bs4`, `markdown_it` 사용 가능).
- Node: `/opt/homebrew/bin/node`.
- 보이는 Chrome의 원격 디버깅 포트: `9335`.
- 당시 전용 Chrome 프로필: `/private/tmp/safetywatch_ux_chrome`.
- 포트·탭 ID·임시 프로필이 재접속 후 그대로라고 가정하지 않는다. 먼저 실행 상태를 확인한다.

임시 폴더에 있던 조작·PDF 내보내기 도구를 이번 기록 시 영구 작업 폴더로 옮겨 두었다.

- [tools/chrome_review.py](tools/chrome_review.py): 탭 조회·탭 선택·가시적 화면 조작·캡처.
- [tools/export_pdf.py](tools/export_pdf.py): 로컬 제안서를 Chrome에서 PDF로 내보내기.

조작 도구의 예:

```sh
/opt/miniconda3/bin/python3 tools/chrome_review.py tabs
/opt/miniconda3/bin/python3 tools/chrome_review.py use <이번에_확인한_탭_ID>
```

이 도구는 선택된 탭을 직접 조작한다. 로컬 시안 작업 전에는 URL이 `file:.../safetywatch_ux_review/workflow.html`인지 확인한다. 실제 사이트는 앞의 조회 전용 제약을 지킨다. 도구의 CDP 연결이 끊겼다고 실제 사이트에 임의 입력을 시도하지 않는다.

변경 후 필요한 경우에만, 결과물 폴더에서 관련 확인을 수행한다.

```sh
/opt/miniconda3/bin/python3 verify_entry_v2.py
/opt/miniconda3/bin/python3 verify_navigation.py
/opt/miniconda3/bin/python3 verify_map.py
```

**검증 스크립트는 로컬 가상 기록을 초기화·변경할 수 있다.** 사용자가 보관하고 싶은 시안 상태를 먼저 백업하고, 같은 Chrome 탭을 조작하는 검증·갤러리 내보내기를 동시에 실행하지 않는다.

화면 또는 제안서를 수정한 뒤 필요한 산출물 갱신:

```sh
/opt/miniconda3/bin/python3 export_gallery_v2.py
/opt/miniconda3/bin/python3 build_report.py
/opt/miniconda3/bin/python3 tools/export_pdf.py
```

CDP로 스크립트 기반 탐색 이력을 확인할 때는 `Runtime.evaluate`에 `userGesture: true`를 포함한다. 이전 검증에서 이를 빠뜨리면 Chrome 이력 처리 때문에 뒤로가기 오탐이 발생했다.

## 11. 다음 작업 후보와 아직 확인하지 않은 점

**현재 수정 요청의 미완료 항목은 없다.** 다음 세션은 사용자의 후속 검토 의견을 받아 그 부분부터 수정하면 된다.

앞으로 검토할 수 있는 과제:

1. 실제 담당자와 주소를 아는 대상 찾기, 관할 후보 탐색, 특정 회차 결함 근거 확인, 원래 지도 복귀를 비교 평가.
2. 실제 행정경계·주소 검색·관리 대상 데이터와의 연결 및 전국 적재 범위 확정.
3. 실제 sBIM/IFC 부재 식별과 결함 좌표·사진·회차·모델 버전의 대응 확인.
4. 공개 요약·공개용 BIM·상세 결함의 공개 정책 확정.
5. BLCM·SSIB의 실제 연계 방식, 역할·관할 권한, 업무 상태와 법정 제출의 경계 확인.
6. 실제 제품 개발 시 작은 화면, 보조기기 접근성, 대용량 지도·모델 성능을 검증.

이 과제들은 자동으로 착수하기로 합의된 추가 개발이 아니다. UX 권고와 실제 연계·운영 정책의 미확인 영역을 구별해 둔 것이다.

## 12. 이번 기록 작업에서 한 일

- 이 Markdown에 대화 요구사항·검토·현재 구현·검증·복구 정보를 통합했다.
- 작업공간 `AGENTS.md`와 결과물 `README.md`에 재개 문서 읽기 경로를 남겼다.
- 현재 Chrome의 로컬 가상 저장소·조회 맥락을 JSON으로 읽어 백업했다.
- 임시 폴더의 도구 두 개를 `tools/`에 보관했다.
- 이번 기록 작업에서는 제품 시안 화면·업무 로직과 실제 사이트 데이터를 변경하지 않았다.


## 13. 최신 추가 기록: 점검 현황·이력 → BIM 직접 연결

2026-09-15 후속 요청: “지도를 통해서만 BIM 접근 가능한데, 점검 현황 및 점검 이력에서도 자연스럽게 연결할 필요”. 기존 현장 근거에도 3D 버튼은 있었으므로 모든 지도 외 진입이 없었다고 단정하지 않고, 현황·이력의 발견성과 업무 맥락을 보강했다.

### 완료한 변경

- 현황·대시보드의 업무 행에 `BIM 보기`, 점검 상세 공통 영역에 `BIM·결함 위치` 추가.
- 점검 이력 목록을 회차별 행으로 표시하고 `이 회차 BIM` 연결.
- 회차·버전 이력에 2025/2026년 BIM과 공유 버전별 근거 진입 추가.
- 업무에서 연 BIM은 `내 업무`에 유지. 지도 BIM과 각각 별도로 마지막 모델·시점·회차 보관.
- 상단에 원래 점검 현황·상세·이력·근거로 돌아가는 버튼. 조회조건·스크롤 유지, 새 세션에서도 원래 업무로 복귀 가능.
- BIM 미등록과 타 기관 권한을 구분. 다른 건물에 주민회관 기록이 섞이지 않게 유지.
- 모바일 회차 비교 표는 표 안에서 가로 이동하도록 보완.

### 코드와 검증

- 새 파일: `business_bim.js`, `verify_business_bim.py`, `business_bim_validation.json`.
- `isAnalysisRoute()`는 모델 진입 경로에 따라 업무/지도 탭을 판단한다. `analysisRoutes.has(screen)`을 무조건 사용하는 변경으로 되돌리지 않는다.
- 모델 진입은 `go(...,{model:...})`, 업무 탭 복원은 `go(...,{workContext:...})`로 전달한다. 원래 화면 맥락을 보관하기 전에 전역 모델을 바꾸면 두 탭의 저장 상태가 서로 덮일 수 있다.
- 업무 BIM은 `workModel.entry`에 원래 화면·조회조건·스크롤을 보관한다. `workContext()`는 업무 모델을 가상 프로필에 포함한다. 순환 참조를 만들지 않도록 entry.context에서 model 필드는 제외한다.
- 새 확인 34개와 기존 105개를 합쳐 총 139개. 최신 결과는 네 검증 JSON을 확인.
- 갤러리 47개. 새 미리보기는 `?tour=42` 현황, `43` 상세, `44` 이력, `45` 회차별 BIM 선택, `46` 이전 회차 BIM.
- 이 보강 전 가상 사용자 상태는 `업무_BIM_보강전_시안상태.json`에 백업했다. 검증용 임시 상태가 사용자의 기존 저장소를 대체하지 않도록 검증 후 복원한다.

기존 7절의 105개·42개 갤러리는 이전 기록이다. 최신 범위는 이 절의 139개·47개를 적용한다. 실제 지도 경계·데이터·모델의 가상 범위와 실제 사이트 조회 전용 제약은 동일하다.

최종 시연: 공무원 로그인 → 점검 현황의 주민회관 BIM → 현황 복귀 → 점검 이력의 2025년 1회차 BIM까지 Chrome에서 확인했다. 검증 전 보관한 사용자 가상 저장소를 복원한 뒤 시연했으며, 최종 시점은 `업무_BIM_연결후_시안상태.json`에 별도 보관했다. 최신 제안서 PDF는 24쪽이다.
